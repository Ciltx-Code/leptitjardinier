{% extends 'base.html.twig' %}

{% block title %}Devis{% endblock %}

{% block body %}
    <h1>Devis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devi.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ devi.date ? devi.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('controller_du_devis_index') }}">back to list</a>

    <a href="{{ path('controller_du_devis_edit', {'id': devi.id}) }}">edit</a>

    {{ include('controller_du_devis/_delete_form.html.twig') }}
{% endblock %}
